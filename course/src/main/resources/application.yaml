server:
  port:
    8082

spring:
  application:
    name: ead-course
  datasource:
    url: jdbc:postgresql://localhost:5432/ead-course?useSSL=false
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update